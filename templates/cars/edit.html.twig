{% extends 'base.html.twig' %}

{% block title %}Edit Cars{% endblock %}

{% block body %}
<div class="container mt-4 mb-4">
    <h1>Edit Cars</h1>

    {{ include('cars/_form.html.twig', {'button_label': 'Update'}) }}

    <a class='btn btn-light mt-4 mb-4' href="{{ path('cars_index') }}">Back</a>

    {{ include('cars/_delete_form.html.twig') }}
</div>
{% endblock %}
